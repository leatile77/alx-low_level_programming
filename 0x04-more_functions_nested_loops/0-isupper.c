#include "main.h"

/**
 * _isupper - Checks if
 * @c is in upper case
 * Return: 0 if lowercase, 1 if uppercase
 */

int _isupper(int c)
{
if (c >= 'A' && C <= 'Z')
{
return (1);
}

else
{
return (0);
}
}
